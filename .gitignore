# macOS junk
.DS_Store
**/.DS_Store

# Environment & secret files
.env
.env.*
!.env.example

# Vercel
.vercel

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archived one-off SQL fix scripts (already applied)
sql-archive/

# Local reference material — kept on disk, not committed (read in-session)
references/*
!references/README.md

# Internal planning — kept local, never served on the public site
docs/
LAUNCH-CHECKLIST.md

# Local Claude Code / editor config (machine-specific)
.claude/
